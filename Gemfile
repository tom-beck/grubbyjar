source 'https://rubygems.org'

gem 'rspec'

# The following gems are not used directly by our code, but are used by
# tests that install gems. If the test suite is run under bundler, these
# gems have to be in that bundle for them to be visible in bundler’s view
# of the world.
gem 'concurrent-ruby', '1.0.4'
